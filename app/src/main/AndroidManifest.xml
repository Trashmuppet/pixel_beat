<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Monochrome Beat is offline-first. We deliberately do not declare
        android.permission.INTERNET — staying compliant with the
        "no cloud, no telemetry, no API keys" product pillars.

        WRITE_EXTERNAL_STORAGE is gated to API ≤ 28 because the export
        pipeline writes to `getExternalFilesDir()` on legacy devices.
        Modern (API 29+) targets use scoped storage and never need the
        permission. Permission matrix lives in docs/06_NAVIGATION_AND_STATE.md.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".MonochromeBeatApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MonochromeBeat">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MonochromeBeat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
