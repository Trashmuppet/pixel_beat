package com.trashmuppet.pixelbeat.ui.theme

import androidx.compose.material3.Typography

/**
 * Typography defaults. Phase 0 ships with Material 3 defaults; the
 * Design Bible (`17_DESIGN_BIBLE-1.md`) will be wired in Phase 6
 * (Performance + Accessibility).
 */
val MonochromeBeatTypography = Typography()
